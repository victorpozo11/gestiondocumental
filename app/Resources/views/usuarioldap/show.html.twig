{% extends 'base.html.twig' %}

{% block body %}
    <h1>Usuarioldap</h1>

    <table>
        <tbody>
            <tr>
                <th>Usuario</th>
                <td>{{ usuarioLdap.usuario }}</td>
            </tr>
            <tr>
                <th>Idusuarioldap</th>
                <td>{{ usuarioLdap.idUsuarioLdap }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('usuarioldap_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('usuarioldap_edit', { 'idUsuarioLdap': usuarioLdap.idUsuarioLdap }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
